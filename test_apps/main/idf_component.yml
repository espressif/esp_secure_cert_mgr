version: "1.0.0"
description: ESP Secure Cert Manager Test Application
dependencies:
  espressif/esp_secure_cert_mgr:
    version: '^2.0.8'
    override_path: '../../' # three levels up, pointing to the directory with the component itself
